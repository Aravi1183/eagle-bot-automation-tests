environments
{

    staging {
        my.webservice.endpoint = "http://localhost:3000"
    }
    prod {
        my.webservice.endpoint = "http://localhost:3000"
    }
    default {
        my.webservice.endpoint = "http://localhost:3000"
        }
    dev {
        my.webservice.endpoint = "http://localhost:3000"
        }
}
serenity.project.name=EagleBotAPI
serenity.test.root=eagleRockHub